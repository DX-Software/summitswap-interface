import { Button } from '@koda-finance/summitswap-uikit'
import styled from 'styled-components'

// eslint-disable-next-line import/prefer-default-export
export const NavStepButton = styled(Button)`
  @media (max-width: 768px) {
    width: 100%;
  }
`
